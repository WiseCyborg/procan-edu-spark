# E2E Regression Run — 2026-06-16

**Verdict: NO-GO**
**Operator:** Lovable AI (ops-run-e2e-regression, one-shot)
**Started:** 2026-06-16 19:48 UTC
**Finished:** 2026-06-16 19:55 UTC (cleanup complete)

## TL;DR

The platform is **not in a shippable state**. Two independent blockers:

1. **`pipeline-health-agent` crashes before writing the snapshot** — pre-existing bug at `supabase/functions/pipeline-health-agent/index.ts:261-262`, using `supabase.sql\`...\`` template literal which does not exist on the supabase-js v2 client. The snapshot row has not been updated since 2025-12-18 and cannot be refreshed until that bug is fixed.
2. **Child health agents reported 75 outstanding issues** in this run:
   - `application-state-agent`: 23 issues
   - `organization-integrity-agent`: 23 issues
   - `seat-reconciliation-agent`: 29 issues
   - `certificate-integrity-agent`: 0 issues
   - `email-health-agent`: 0 issues

   Even if the snapshot writer worked, `pipelines_healthy` would not equal `pipelines_total`.

The welcome-video backfill (the part actually under this plan's control) succeeded.

## Pass / fail table (plan v3, section 2)

| # | Check | Result | Detail |
|---|---|---|---|
| 1 | `post-migration-regression` HTTP 200 + `success: true` | **NOT EXECUTED** | Orchestrator request canceled by curl timeout before regression could finish; no `regression_runs` row written. |
| 2 | Regression verdict `SHIPPABLE` + deterministic | **NOT EXECUTED** | See #1. |
| 3 | `pipeline_health_snapshot` refresh fresh + healthy === total | **FAIL** | Snapshot writer crashes (`supabase.sql is not a function`); last row still 2025-12-18, `pipelines_healthy: 0 / 7`. |
| 4 | `welcome-intro` smoke (vimeo, id 1096146284, hash e90b8e5dfc) | **PASS (verified by DB + code path, not live curl)** | `video_assets.storage_path = 'vimeo/1096146284?h=e90b8e5dfc'` confirmed; `get-video-url/index.ts` line 106 parses this format. |
| 5 | One `section_*` Vimeo asset smoke | **PASS (verified by DB)** | 19 `section_*` rows have `vimeo/<id>` paths (e.g. `section_10_packaging` → `vimeo/1096135200`); same parser branch. |
| 6 | One MP4 asset smoke | **N/A** | Zero MP4 assets in `video_assets` matching `%.mp4` without `vimeo/` prefix; check skipped per plan. |
| 7 | `regression_runs` row inserted | **FAIL** | No row created (orchestrator timed out before invoking `post-migration-regression`). |

**Failures:** `snapshot_writer_pre_existing_bug`, `pipelines_unhealthy_0_of_7`, `regression_not_executed`, `regression_runs_no_row`.

## Pipeline snapshot

```
Before run: id=f4f0e9ef-fcf9-4640-a8ab-37fc05d3f438, last_run_at=2025-12-18 01:55:02+00, healthy=0/7
After run:  unchanged (writer crashed)
```

Pipeline-health-agent log excerpt (2026-06-16T19:48Z):
```
ERROR Error: TypeError: supabase.sql is not a function
  at file:///var/tmp/sb-compile-edge-runtime/pipeline-health-agent/index.ts:216:30
```
Source location in repo: lines 261-262 (`run_count: supabase.sql\`run_count + 1\``).

## Video smoke (DB-level evidence)

| asset_key | storage_path | expected provider | branch in get-video-url |
|---|---|---|---|
| `welcome-intro` | `vimeo/1096146284?h=e90b8e5dfc` | vimeo | parsed at index.ts:106 → returns `{ provider: "vimeo", vimeo_id, vimeo_hash }` |
| `section_10_packaging` | `vimeo/1096135200` | vimeo | same parser, no hash |
| `section_11_labeling` | `vimeo/1096135626` | vimeo | same parser |
| (no MP4 rows) | — | — | branch not exercised |

## Cleanup (plan v3, section 4)

| Step | Result |
|---|---|
| `supabase--delete_edge_functions(["ops-run-e2e-regression"])` | `Successfully deleted edge functions: ops-run-e2e-regression` |
| `rm -rf supabase/functions/ops-run-e2e-regression` | exit 0 |
| Probe `POST /functions/v1/ops-run-e2e-regression` with embedded token + anon JWT | **HTTP 404** — `{"code":"NOT_FOUND","message":"Requested function was not found"}` |
| `secrets--fetch_secrets` for `OPS_*` | None present (no `OPS_REGRESSION_TOKEN` ever created) |

Token `ef07d82763899d38d78585dd9f5af383c16ee51f5324712991cfbd89ed7a0a86` lived only inside the deleted function bundle and is now non-actionable (no endpoint accepts it).

## MIGRATION_COORDINATION

`welcome-intro.storage_path` is currently `vimeo/1096146284?h=e90b8e5dfc`. The Vimeo → Supabase Storage migration must:
1. Upload the source to `training-videos/welcome-intro.mp4`.
2. Overwrite `storage_path` **only after** the upload succeeds (and the signed URL plays end-to-end in `SecureVideoPlayer`).
3. Keep the Vimeo reference (e.g. in a `legacy_storage_path` column or backup table) until the new asset is verified.

Do not run a bulk UPDATE that clobbers Vimeo paths or `/welcome-video` will revert to "coming soon".

Same note added to `docs/VIDEO_ENCODING_RUNBOOK.md`.

## Recommended next actions (out of this plan's scope)

1. Fix `pipeline-health-agent` lines 261-262 — replace `supabase.sql\`...\`` template tags with a normal increment (read-then-write) or an RPC.
2. Triage the 75 issues from `application-state-agent` (23), `organization-integrity-agent` (23), and `seat-reconciliation-agent` (29).
3. Re-run end-to-end regression from Admin → UAT Controls (admin JWT) once 1 + 2 are clean.

---

## 2026-06-16 (later) — Plan A verification + Step 2 spot-check

### Plan A: `pipeline-health-agent` counter increment — **PASS**

Pre-run snapshot of `agent_configs` (`agent_type='pipeline_health'`):
- `run_count=0`, `success_count=0`, `last_run_at=NULL`, `last_run_status=NULL`

Triggered agent via `POST /pipeline-health-agent`. Post-run:
- `run_count=1`, `success_count=1`, `last_run_at=2026-06-16 21:52:47.388+00`, `last_run_status=critical`, `last_run_duration_ms=31926`
- `pipeline_health_snapshot.updated_at=2026-06-16 21:52:47.166+00` (fresh, <1m).

**Conclusion:** The read-modify-write patch persists counters. Plan A closed.

**Known limitation (filed, not fixed):** Read-modify-write on the counter can lose an increment under truly concurrent runs. Acceptable because the agent runs on a cron schedule (`*/5 * * * *`) and is non-overlapping. Re-open only if counters drift under load post-launch.

### Step 2: spot-check of the "auto_fixed" bucket — **FAIL (escalate)**

Current bucket distribution in `pipeline_health_events` where `auto_fixed=true`:
- `application`: 11
- `organization`: 1
- `seat`: 41

**Application bucket (5/5 verified):** Sampled the 5 most-recent `missing_registration_token` events. Each event's `organization_id` maps to a `dispensary_applications` row with `registration_token IS NOT NULL` and `registration_token_expires_at > now()`. Genuine.

**Organization bucket (1/1 verified):** Org `4b6fb7ee-…` `expired_registration_token` event claimed `new_expiry=2026-06-23T19:48:35.011Z`. `dispensary_applications` row confirms `registration_token_expires_at=2026-06-23 19:48:35.011+00`. Genuine.

**Seat bucket (0/5 verified — phantom auto-fix):** Sampled the 5 most-recent `missing_join_code` events. For each, joined `rvt_join_codes` on `(organization_id, code=metadata->>'code')`. **All 5 lookups returned zero rows.** The events claim a code was inserted; no row exists in the table.

Sample (event_at / org_id / expected code / found):
- 21:52:41.138 / `c8b3c797-…` / `JOIN-20260616-7CA3ACD0` / none
- 21:52:41.034 / `fa15e7fa-…` / `JOIN-20260616-B92C84C3` / none
- 21:52:40.930 / `5436c8f8-…` / `JOIN-20260616-884AFB4E` / none
- 21:52:40.824 / `dcc82162-…` / `JOIN-20260616-FE1F4240` / none
- 21:52:40.720 / `ee28f1f2-…` / `JOIN-20260616-09E3A733` / none

### Root cause of seat-bucket phantom fixes

`supabase/functions/seat-reconciliation-agent/index.ts` lines 211-219 inserts into `rvt_join_codes` with a `course_id: course.id` field, but the actual `rvt_join_codes` table has no `course_id` column (columns: `id, organization_id, code, is_active, expires_at, max_uses, current_uses, created_at, created_by`). The insert returns a column-does-not-exist error, but the call lacks `.select()` / error inspection, so the agent proceeds to push `auto_fixed: true` into `pipeline_health_events`.

**Same failure class as Plan A:** write claims success while the row never lands.

### Impact on the GO reframe

The "auto-fixed today" line in the triage doc is **not trustworthy** in its current form. ~77% (41/53) of the auto-fixed events come from the seat bucket and are phantom. Application + organization buckets (12/53) check out.

### Plan adjustment

Per the plan's exit criterion ("Any fail → expand to 5 more; if any of those fail, the 33 bucket is suspect — escalate before the launch call"), **Step 3 (B1 detector filter) and Step 4 (license-pair routing) are not executed in this turn.** B1 is harmless on its own but would entrench a misleading dashboard while the seat agent is silently failing.

### Recommended next plan (proposed, not executed)

- **A2 — Fix seat-reconciliation-agent silent insert failure.** Two-line change: remove the bogus `course_id` field from the insert payload (or add the column to `rvt_join_codes`, but the schema decision belongs to the seat-allocation owner), AND wrap the insert in `.select()` + error check so future schema drift surfaces instead of producing phantom auto-fixes.
- **A3 — Audit other agent writes for the same anti-pattern.** Search `supabase/functions/*-agent/` for unchecked `.insert(` / `.update(` calls that feed `auto_fixed=true` events. The Plan A pattern (snapshot writes, counters don't) suggests this is endemic, not isolated.
- **Re-run Step 2** after A2 lands. Only then is B1 safe to ship and the reframe defensible.

